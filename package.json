{
  "name": "dpc-web",
  "private": true,
  "version": "0.1.0",
  "description": "Downtown Pour Collective interim website (member + partner landing pages).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node scripts/test-stripe-webhook-paths.mjs"
  },
  "dependencies": {
    "resend": "^4.0.0",
    "stripe": "^17.7.0"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  }
}
